import { AddScrollDirective } from './add-scroll.directive';

describe('AddScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AddScrollDirective();
    expect(directive).toBeTruthy();
  });
});
